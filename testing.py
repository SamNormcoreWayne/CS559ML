class Testing:
    mean_one = 0.0
    mean_zero = 0.0
    var_one = 0.0
    var_zero = 0.0
    def __init__(self, mean_one, mean_zero, var_one, var_zero):
        Testing.mean_one = mean_one
        Testing.mean_zero = mean_zero
        Testing.var_one = var_one
        Testing.var_zero = var_zero
    
